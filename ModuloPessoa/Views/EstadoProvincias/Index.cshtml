@model IEnumerable<ModuloPessoa.EstadoProvincia>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Regiao.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CodigoEstadoProvincia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SomenteEstadoProvinciaFlag)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Regiao.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoEstadoProvincia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SomenteEstadoProvinciaFlag)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.EstadoProvinciaID }) |
                @Html.ActionLink("Details", "Details", new { id=item.EstadoProvinciaID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.EstadoProvinciaID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
